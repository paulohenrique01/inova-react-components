import { makeStyles } from '@material-ui/core/styles';
var useStyles = makeStyles(function (theme) {
  return {
    button: {
      margin: theme.spacing(2)
    }
  };
});
export default useStyles; //npm run compile